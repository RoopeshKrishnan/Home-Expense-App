<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HomeExpense" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:textColorSecondary">@color/purple_700</item>



        <item name="fontFamily">@font/varela_round</item>
        <!-- Customize your theme here. -->

    </style>

    <!-- Splash Screen  theme.
    -->
    <style name="SplashScreenTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.HomeExpense.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.HomeExpense.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Alert Dialoge style-->
    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@android:color/holo_orange_dark</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/purple_700</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/background_dialog</item>
        <item name="alertDialogCenterButtons">true</item>

    </style>

    <!-- toolbar backbutton style-->
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/purple_700</item>
        //toolbar title color
        <item name="android:textColorPrimary">@android:color/white</item>

    </style>


// switch compact color
    <style name="BrandedSwitch.control" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/purple_700</item>
        <item name="colorSwitchThumbNormal">@color/teal_700</item>
    </style>





</resources>